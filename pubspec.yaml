name: ban_pa_lao_procurement
description: ระบบสร้างเอกสารจัดซื้อจัดจ้างอัตโนมัติ

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.3.0 # SQLite บน Windows desktop
  path: ^1.9.0
  path_provider: ^2.1.0
  docx_template: ^0.4.0 # หรือ archive + xml สำหรับ manual docx manipulation
  archive: ^3.4.0
  intl: ^0.19.0
  file_picker: ^8.0.0
  pdf: ^3.10.0
  printing: ^5.12.0
  provider: ^6.1.0 # state management

flutter:
  uses-material-design: true
  assets:
    - assets/templates/
